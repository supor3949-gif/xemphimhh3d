:root{
  --bg:#0b1224;
  --panel:rgba(17,24,39,.55);
  --panel2:rgba(2,6,23,.55);
  --panel3:rgba(11,16,32,.85);
  --text:#e5e7eb;
  --muted:#94a3b8;
  --border:rgba(255,255,255,.12);
  --border2:rgba(255,255,255,.06);
  --green:#22c55e;
  --greenDark:#062a16;
  --blue:#38bdf8;
  --shadow:0 12px 28px rgba(0,0,0,.35);
  --radius:18px;
}
*{box-sizing:border-box}
body{
  margin:0;font-family:Arial,sans-serif;color:var(--text);
  background:
    radial-gradient(1200px 600px at 50% -10%, rgba(56,189,248,.10), transparent 60%),
    radial-gradient(1000px 500px at 20% 0%, rgba(34,197,94,.10), transparent 55%),
    var(--bg);
}
a{color:inherit;text-decoration:none}
.container{max-width:1280px;margin:0 auto;padding:14px 16px 30px}
.panel{
  background:var(--panel);border:1px solid var(--border2);
  border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden;
}
.siteHeader{
  position:sticky;top:0;z-index:50;
  background:rgba(2,6,23,.88);backdrop-filter: blur(12px);
  border-bottom:1px solid var(--border2);
}
.headerInner{
  max-width:1280px;margin:0 auto;padding:12px 16px;
  display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap;
}
.brand{display:flex;align-items:center;gap:10px;min-width:220px}
.logo{
  width:42px;height:42px;border-radius:12px;
  box-shadow:0 10px 24px rgba(0,0,0,.35);
  display:grid;place-items:center;
  background:rgba(56,189,248,.15);
  border:1px solid rgba(56,189,248,.25);
}
.brandText{display:flex;flex-direction:column;line-height:1.1}
.brandName{font-size:18px;font-weight:900;letter-spacing:.3px}
.brandTag{font-size:12px;color:var(--muted);margin-top:2px}

.pill{
  background:rgba(11,16,32,.85);
  border:1px solid var(--border);
  border-radius:14px;
  padding:10px 12px;
  font-size:13px;color:#cbd5e1;
  white-space:nowrap;
}
.btn{
  border:none;
  background:rgba(56,189,248,.18);
  border:1px solid rgba(56,189,248,.35);
  color:#dbeafe;
  padding:10px 12px;border-radius:14px;
  cursor:pointer;font-weight:800;
}
.btn:hover{filter:brightness(1.05)}
.btn.green{
  background:var(--green);
  color:var(--greenDark);
  border:1px solid rgba(34,197,94,.55);
}

/* ===== HOME GRID LAYOUT ===== */
.homeGrid{
  display:grid;
  grid-template-columns: 1fr 320px;
  gap:14px;
}
@media (max-width:1100px){
  .homeGrid{grid-template-columns:1fr}
}

/* ===== TOP SLIDER ===== */
.slider{
  display:flex;gap:10px;overflow:auto;padding:12px;
  scroll-snap-type:x mandatory;
}
.slider::-webkit-scrollbar{height:10px}
.slider::-webkit-scrollbar-thumb{background:rgba(255,255,255,.10);border-radius:99px}
.cardMini{
  flex:0 0 210px;
  scroll-snap-align:start;
  border-radius:16px;
  border:1px solid rgba(255,255,255,.10);
  background:rgba(2,6,23,.55);
  overflow:hidden;
}
.cardMini img{width:100%;height:120px;object-fit:cover;display:block}
.cardMini .pad{padding:10px}
.badge{
  display:inline-block;
  font-size:11px;font-weight:900;
  padding:4px 8px;border-radius:999px;
  background:rgba(34,197,94,.18);
  border:1px solid rgba(34,197,94,.35);
  color:#d1fae5;
}

/* ===== SCHEDULE ===== */
.scheduleHeader{
  padding:12px 14px;border-bottom:1px solid var(--border2);
  display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;
  background:rgba(2,6,23,.55);
}
.tabs{display:flex;gap:8px;flex-wrap:wrap}
.tab{
  padding:10px 12px;border-radius:14px;cursor:pointer;font-weight:900;font-size:12px;
  background:rgba(2,6,23,.55);
  border:1px solid rgba(255,255,255,.10);
}
.tab.active{
  background:rgba(56,189,248,.12);
  border-color:rgba(56,189,248,.35);
}
.scheduleGrid{
  display:grid;
  grid-template-columns:repeat(7,1fr);
  gap:10px;
  padding:14px;
}
@media (max-width:1100px){ .scheduleGrid{grid-template-columns:repeat(3,1fr)} }
@media (max-width:680px){ .scheduleGrid{grid-template-columns:repeat(2,1fr)} }

.dayBox{
  border-radius:16px;
  border:1px solid rgba(255,255,255,.10);
  background:rgba(2,6,23,.55);
  overflow:hidden;
}
.dayTop{
  padding:10px 12px;border-bottom:1px solid rgba(255,255,255,.08);
  display:flex;align-items:center;justify-content:space-between;
  background:rgba(11,16,32,.55);
}
.dayTop b{font-size:12px}
.dayTop span{font-size:11px;color:var(--muted)}
.dayList{padding:10px;display:flex;flex-direction:column;gap:8px}
.itemRow{
  display:flex;gap:8px;align-items:center;
  border:1px solid rgba(255,255,255,.08);
  background:rgba(0,0,0,.18);
  padding:8px;border-radius:14px;
}
.itemRow img{width:44px;height:44px;border-radius:10px;object-fit:cover}
.itemRow .t{font-weight:900;font-size:12px}
.itemRow .s{font-size:11px;color:var(--muted)}

/* ===== RIGHT RANK ===== */
.rankWrap{padding:12px}
.rankTitle{font-weight:900;margin:0 0 10px;color:#7dd3fc}
.rankList{display:flex;flex-direction:column;gap:10px}
.rankItem{
  display:flex;gap:10px;align-items:center;
  border:1px solid rgba(255,255,255,.10);
  background:rgba(2,6,23,.55);
  border-radius:16px;padding:10px;
}
.rankItem img{width:48px;height:64px;border-radius:12px;object-fit:cover}
.rankNum{width:22px;text-align:center;font-weight:900;color:#93c5fd}

/* ===== FOOTER ===== */
.footerNote{
  max-width:1280px;margin:0 auto;
  padding:0 16px 26px;color:var(--muted);
  font-size:11px;line-height:1.55;
}
.footerGrid{
  max-width:1280px;margin:0 auto;
  padding:0 16px 10px;
  display:grid;grid-template-columns:repeat(3,1fr);gap:10px;
}
@media (max-width:900px){ .footerGrid{grid-template-columns:1fr} }
.footerCard{
  border:1px solid rgba(255,255,255,.10);
  background:rgba(2,6,23,.55);
  border-radius:16px;padding:12px;
  font-size:12px;color:#cbd5e1;
}
.footerCard b{display:block;margin-bottom:6px}

/* ===== ADMIN ===== */
.form{
  padding:14px;display:grid;gap:10px;
}
.inp, textarea, select{
  width:100%;
  background:rgba(2,6,23,.55);
  border:1px solid rgba(255,255,255,.12);
  border-radius:14px;
  color:var(--text);
  padding:10px 12px;
  outline:none;
}
textarea{min-height:120px;resize:vertical}
.row2{display:grid;grid-template-columns:1fr 1fr;gap:10px}
@media (max-width:860px){ .row2{grid-template-columns:1fr} }
